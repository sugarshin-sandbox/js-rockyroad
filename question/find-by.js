/*
  オブジェクトを要素に持つ配列を受け取り、
  第2引数で渡されたオブジェクトのプロパティと値が
  すべて一致するオブジェクトを返す
  見つからない場合は`null`を返す

  @param {Array} array
  @param {Object} object
  @returns {Object}
*/

export default function(array, object) {

}
